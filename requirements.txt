# Kratib 3DGS -- pip dependencies, installed inside the `kratib` conda env.
#
# Three things this file does NOT cover, because they are not pip packages
# (see docs/setup_rog.md):
#   - NVIDIA driver        system install
#   - VS2022 Build Tools   "Desktop development with C++" -- gsplat needs cl.exe
#   - COLMAP               Windows CUDA build, on PATH -- ns-process-data calls it
#
# Install order matters:
#   1. conda env create -f environment.yml     (Python, in-env CUDA, ffmpeg)
#   2. conda activate kratib
#   3. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   4. pip install -r requirements.txt
#   5. pip install gsplat --no-build-isolation
#      ^ separate + last: its build imports torch and needs nvcc + cl.exe on PATH.
#        Run the first `ns-train` from an "x64 Native Tools Command Prompt for VS 2022".

# --- Stage 01 (video -> frames) ---
av>=12.0            # PyAV: in-process decode, HEVC-safe (cv2.VideoCapture is not)
opencv-python>=4.8
numpy>=1.26

# --- Stages 02-05 (COLMAP wrangling, training, export, render) ---
nerfstudio>=1.1.5

# torch / torchvision -- installed separately (step 3) from the cu124 index, not here.
# gsplat              -- installed separately (step 5) with --no-build-isolation.
